/**
 * @param {number[][]} mat
 * @return {number[][]}
 */
var updateMatrix = function (mat) {
  
	mat.forEach((x, i) => {
		x.forEach((y, j) => {

		});
	});
  return mat;
};
console.log(
	updateMatrix([[0,0,0],[0,1,0],[1,1,1]])
);
